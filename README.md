-This application uses the MVI architecture with this library: Hilt for DI, Ktor for Rest Api,
Navigation, Compose for UI.
-To run this project just install andorid studio and import the porject then hit the run button
-Implement Room for caching
-Integrate CD/DI

The App will fetch data from remote then if there is no netwrok he will get the last stored request for the home screen and once there are network he immediatly will try to get the current status.
Add favourite screen containe field to type the country if manage to get the info about the country a small snackbar will appear and this location will be saved in the DB
Favourite location will show the stored favourite location
